# Invoke the dispatcher directly.  Keeping the weighted roll in an event makes
# it possible to test the whole succession selection with `event succession_wars.1`.
on_new_monarch = {
	events = {
		succession_wars.1
	}
}
