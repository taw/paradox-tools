#!/usr/bin/env ruby

require_relative "../lib/division_designer"
require "set"

class AvailableYearsCLI
  def initialize
    @db = Database.new
  end

  def call(args)
    raise "This command takes no arguments" unless args.empty?
    puts @db.years
  end
end

AvailableYearsCLI.new.call(ARGV)
