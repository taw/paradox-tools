#!/usr/bin/env ruby

require "pry"
require_relative "../lib/paradox"

unless ARGV.size >= 1
  STDERR.puts "Usage: #{$0} <root> [<mod1> <mod2> ... ]"
  exit 1
end

$game = ParadoxGame.new(*ARGV)
$game.instance_eval { binding.pry }
