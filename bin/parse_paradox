#!/usr/bin/env ruby

require_relative "../lib/paradox"

unless ARGV.size >= 1
  STDERR.puts "Usage: #{$0} <path1.txt> ..."
  exit 1
end

ARGV.each do |path|
  p ParadoxModFile.new(path: path).parse!
end
